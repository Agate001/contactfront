export interface UserInfo {
  username: string;
  email: string;
  password: string;
}
export interface Token {
  token: string;
}

export interface Contact {
  id: number;
  name: string;
  contactEmail: string;
  number: string;
}
